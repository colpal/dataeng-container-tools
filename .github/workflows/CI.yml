# This file is autogenerated by maturin v1.9.6
# To update, run
#
#    maturin generate-ci github
#
name: CI Maturin

on:
  push:
    branches:
      - feat/dect
  workflow_dispatch:

permissions:
  contents: read

jobs:
  linux:
    runs-on: ${{ matrix.platform.runner }}
    strategy:
      matrix:
        platform:
          - runner: ubuntu-22.04
            target: x86_64
            target_triple: x86_64-unknown-linux-gnu  # https://github.com/PyO3/maturin-action/blob/494b36ec40fe8350f900229e7d2c8ca7f896a67a/src/index.ts#L210
          # - runner: ubuntu-22.04
          #   target: x86
          # - runner: ubuntu-22.04
          #   target: aarch64
          # - runner: ubuntu-22.04
          #   target: armv7
          # - runner: ubuntu-22.04
          #   target: s390x
          # - runner: ubuntu-22.04
          #   target: ppc64le
    steps:
      - uses: actions/checkout@v4

      - name: Install UV
        uses: astral-sh/setup-uv@61cb8a9741eeb8a550a1b8544337180c0fc8476b # v7.2.0
        with:
          version: "0.9.24"
          python-version: "3.13"
          activate-environment: true

      - name: Build wheels
        # uses: PyO3/maturin-action@v1
        # with:
        #   target: ${{ matrix.platform.target }}
        #   args: --release --out dist
        #   sccache: ${{ !startsWith(github.ref, 'refs/tags/') }}
        #   manylinux: auto
        run: uvx maturin build --target ${{ matrix.platform.target }} --release --out dist

      - name: Upload wheels
        uses: actions/upload-artifact@v4
        with:
          name: wheels-linux-${{ matrix.platform.target }}
          path: dist

  windows:
    runs-on: ${{ matrix.platform.runner }}
    strategy:
      matrix:
        platform:
          - runner: windows-latest
            target: x64
            target_triple: x86_64-pc-windows-msvc
          # - runner: windows-latest
          #   target: x86
    steps:
      - uses: actions/checkout@v4

      - name: Install UV
        uses: astral-sh/setup-uv@61cb8a9741eeb8a550a1b8544337180c0fc8476b # v7.2.0
        with:
          version: "0.9.24"
          python-version: "3.13"
          activate-environment: true

      - name: Build wheels
        # uses: PyO3/maturin-action@v1
        # with:
        #   target: ${{ matrix.platform.target }}
        #   args: --release --out dist
        #   sccache: ${{ !startsWith(github.ref, 'refs/tags/') }}
        run: uvx maturin build --target ${{ matrix.platform.target }} --release --out dist

      - name: Upload wheels
        uses: actions/upload-artifact@v4
        with:
          name: wheels-windows-${{ matrix.platform.target }}
          path: dist

  macos:
    runs-on: ${{ matrix.platform.runner }}
    strategy:
      matrix:
        platform:
          # - runner: macos-13
          #   target: x86_64
          - runner: macos-14
            target: aarch64
            target_triple: aarch64-apple-darwin
    steps:
      - uses: actions/checkout@v4

      - name: Install UV
        uses: astral-sh/setup-uv@61cb8a9741eeb8a550a1b8544337180c0fc8476b # v7.2.0
        with:
          version: "0.9.24"
          python-version: "3.13"
          activate-environment: true

      - name: Build wheels
        # uses: PyO3/maturin-action@v1
        # with:
        #   target: ${{ matrix.platform.target }}
        #   args: --release --out dist
        #   sccache: ${{ !startsWith(github.ref, 'refs/tags/') }}
        run: uvx maturin build --target ${{ matrix.platform.target }} --release --out dist

      - name: Upload wheels
        uses: actions/upload-artifact@v4
        with:
          name: wheels-macos-${{ matrix.platform.target }}
          path: dist
